<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding ="10dp"
    android:id="@+id/row_rec">

    <de.hdodenhof.circleimageview.CircleImageView
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:id="@+id/row_rec_photo"
        android:src="@mipmap/ic_launcher"
        android:layout_centerVertical="true"/>

    <ImageView
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:id="@+id/row_rec_image"
        android:layout_marginStart="5dp"
        android:text="This is temporary text for messaging."
        android:textColor="@color/black"
        android:textSize="12sp"
        android:padding="10dp"
        android:background="@drawable/rcv_msg_box"
        android:layout_toRightOf="@id/row_rec_photo"
        android:layout_marginLeft="5dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/row_rec_time"
        android:text="Msg time"
        android:textSize="12sp"
        android:textColor="@color/grey_hint"
        android:layout_toRightOf="@id/row_rec_photo"
        android:layout_below="@id/row_rec_image"
        android:layout_marginTop="5dp"
        android:layout_marginStart="10dp"
        android:layout_marginLeft="10dp" />



</RelativeLayout>