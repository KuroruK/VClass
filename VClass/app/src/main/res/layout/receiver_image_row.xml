<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/row_rec"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginVertical="3dp"
    android:background="@drawable/rcv_msg_box"
    android:elevation="3dp"
    android:paddingHorizontal="10dp"
    android:paddingVertical="5dp">

    <TextView
        android:id="@+id/file_rec_sender"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Sender"
        android:textColor="@color/black"
        android:textSize="12sp"
        android:textStyle="bold" />

    <RelativeLayout
        android:id="@+id/row_rec_image0"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/file_rec_sender"
        android:layout_centerHorizontal="true"
        android:layout_marginVertical="2dp">

        <ImageView
            android:id="@+id/row_rec_image"
            android:layout_width="180dp"
            android:layout_height="120dp" />
    </RelativeLayout>

    <TextView
        android:id="@+id/row_rec_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/row_rec_image0"
        android:text="time"
        android:textColor="@color/grey_hint"
        android:textSize="12sp" />


</RelativeLayout>